import { ProductosProveedor } from './productos-proveedor';

describe('ProductosProveedor', () => {
  it('should create an instance', () => {
    expect(new ProductosProveedor()).toBeTruthy();
  });
});
